import SwiftUI

/// The app's root destination is Daily Plan. Existing numbered feature routes
/// remain independent and are available through menus and Command-K.
struct HomeView: View {
    var body: some View { DailyPlanView() }
}
